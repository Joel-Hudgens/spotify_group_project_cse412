# SPOTIFY EMULATOR DATABASE PROJECT
Team Name: Chicken Drumsticks

## Setting up Spotify PostgreSQL DB locally
In your terminal:
Navigate to this repo, then run:
`createdb spotify`
`psql spotify -f ddl.sql`
`psql spotify -f load_data.sql`
